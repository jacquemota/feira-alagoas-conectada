import type { ReactNode } from "react";
export function CabecalhoPagina({ titulo, descricao, acao }: { titulo: string; descricao?: string; acao?: ReactNode }) { return <div className="border-b bg-card"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-4 py-8 sm:flex-row sm:items-end sm:justify-between sm:px-6 lg:px-8"><div><h1 className="text-3xl font-bold text-marinho sm:text-4xl">{titulo}</h1>{descricao && <p className="mt-2 max-w-2xl text-muted-foreground">{descricao}</p>}</div>{acao}</div></div>; }
export function Container({ children, className = "" }: { children: ReactNode; className?: string }) { return <div className={`mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8 ${className}`}>{children}</div>; }
export function Vazio({ titulo, descricao, acao }: { titulo: string; descricao: string; acao?: ReactNode }) { return <div className="rounded-lg border border-dashed bg-card px-6 py-14 text-center"><h2 className="text-xl font-bold text-marinho">{titulo}</h2><p className="mx-auto mt-2 max-w-md text-sm text-muted-foreground">{descricao}</p>{acao && <div className="mt-5">{acao}</div>}</div>; }
