import { createFileRoute } from "@tanstack/react-router";import { FormularioEvento } from "@/components/formulario-evento";import { PainelLayout,TituloPainel } from "@/components/painel-layout";import { useEstado } from "@/lib/loja";export const Route=createFileRoute("/organizador/eventos/$id/editar")({head:()=>({meta:[{title:"Editar feirinha — FeiraFácil Alagoas"},{name:"description",content:"Edite os dados de uma feirinha demonstrativa."},{property:"og:title",content:"Editar feirinha — FeiraFácil Alagoas"},{property:"og:description",content:"Atualize informações do evento."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Pagina});function Pagina(){const {id}=Route.useParams(),estado=useEstado();const evento=estado.eventos.find(e=>e.id===id);return <PainelLayout tipo="organizador"><TituloPainel titulo="Editar feirinha" {...(evento ? { descricao: evento.nome } : {})}/>{evento?<FormularioEvento existente={evento}/>:<p>Evento não encontrado.</p>}</PainelLayout>}
